# Copy to .env on the server (.env is gitignored) and set your hostname(s).
# One name, or several comma-separated — each gets its own Let's Encrypt cert.
#   SITE_HOST=panel.komakallio.fi
#   SITE_HOST=panel.komakallio.fi, kk.dy.fi
SITE_HOST=panel.komakallio.fi
